"""
Setup configuration for BioRSP package.
This file is kept for backwards compatibility, but pyproject.toml is the main config.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
